import { Farmer } from "./types";

interface Props {
  onSearch: (query: string) => void;
}

export default function FarmerSearch() {
  return (
    <>
      <form>
        <input />
      </form>
    </>
  );
}
